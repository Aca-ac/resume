spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${MYSQL_DATABASE:resume_assistant}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USER:resume}
    password: ${MYSQL_PASSWORD:resume123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: ${REDIS_PASSWORD:}

app:
  expose-error-details: true
  ai:
    qwen:
          api-key: ${DASHSCOPE_API_KEY:${QWEN_API_KEY:}}

logging:
  level:
    com.resume: debug

# OCR配置
<<<<<<< HEAD
app:
  ai:
    qwen:
      api-key: ${DASHSCOPE_API_KEY:${QWEN_API_KEY:}}
=======
>>>>>>> origin/main
