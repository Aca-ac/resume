# Sprint 1 开发计划

## 一、迭代目标

完成用户注册登录、简历导入导出、AI简历智能分析三大核心功能模块的开发。

## 二、任务清单

### 1. 用户注册与登录模块

**后端任务：**
- 设计用户表结构（user表：id、email、password、name、phone、birthday、education、work_years、city、created_at、updated_at）
- 实现邮箱验证码发送接口（调用第三方邮件服务）
- 实现用户注册接口（邮箱+密码+验证码验证）
- 实现用户登录接口（邮箱+密码验证，生成JWT Token）
- 实现Token刷新接口（Refresh Token机制）
- 实现个人信息CRUD接口
- 集成Spring Security进行权限控制

**前端任务：**
- 开发注册页面（邮箱、密码、验证码输入）
- 开发登录页面（邮箱、密码输入）
- 开发个人中心页面（姓名、手机号、出生日期、学历、工作年限、城市表单）
- 实现Token过期自动刷新逻辑
- 实现个人信息与简历模板的关联自动填充

---

### 2. 简历导入与导出模块

**后端任务：**
- 设计简历表结构（resume表：id、user_id、content、created_at、updated_at）
- 实现Word文档解析接口（.doc/.docx格式文本提取）
- 实现PDF文档解析接口（文本提取）
- 实现JPG图片OCR识别接口（调用OCR服务）
- 实现简历导出为Word接口
- 实现简历导出为PDF接口
- 设计文件存储方案（本地存储或云存储）

**前端任务：**
- 开发简历导入页面（支持选择.doc/.docx/.pdf/.jpg文件）
- 开发OCR识别结果展示与手动修正页面
- 实现导入进度与状态提示
- 开发简历导出功能（支持选择.docx/.pdf格式）
- 实现导出文件下载

---

### 3. AI简历智能分析模块

**后端任务：**
- 设计分析记录表结构（analysis_record表：id、user_id、resume_id、score_json、suggestions、created_at）
- 实现简历多维度评分算法（教育背景、工作经历、项目经验、技能匹配、成果量化、排版规范）
- 实现大模型API调用封装（阿里云API）
- 实现AI分析接口（调用大模型生成修改建议）
- 实现分析历史记录查询接口

**前端任务：**
- 开发AI分析触发按钮与加载状态提示
- 使用ECharts实现雷达图评分展示（6个维度）
- 开发修改建议列表展示（原文定位、问题描述、优化建议、修改示例）
- 实现"应用建议"一键应用功能
- 实现"忽略"跳过建议功能
- 开发分析历史记录页面

---

### 4. 基础设施与公共模块

**后端任务：**
- 搭建SpringBoot项目基础框架
- 配置数据库连接（MySQL）
- 集成MyBatis-Plus
- 配置Knife4j接口文档
- 实现统一返回封装
- 实现全局异常处理
- 实现参数校验

**前端任务：**
- 搭建Vue3项目基础框架（Vite + Vue3 + Element Plus + Pinia）
- 配置路由与权限拦截
- 开发全局布局组件
- 配置Axios请求封装与拦截器
- 配置样式主题

---

## 三、验收标准

### 用户注册与登录
- AC1.1: 用户输入有效邮箱和密码后，系统发送6位数字验证码至该邮箱，输入正确验证码后完成注册
- AC1.2: 注册成功后，用户可使用邮箱+密码登录，登录后生成JWT Token用于后续请求鉴权
- AC1.3: Token过期后，系统自动使用Refresh Token刷新，用户无感知
- AC1.4: 登录成功后进入个人中心，用户可填写/修改：姓名、手机号、出生日期、最高学历、工作年限、所在城市
- AC1.5: 个人中心信息保存后，在简历模板中可自动读取并填充

### 简历导入与导出
- AC2.1: 用户点击"导入简历"，支持选择 .doc / .docx / .pdf / .jpg 格式文件上传
- AC2.2: Word文件导入后，系统能提取正文文本，保留段落结构，准确率≥90%
- AC2.3: PDF文件导入后，系统能提取正文文本，准确率≥90%
- AC2.4: JPG文件导入后，系统调用OCR识别文字，准确率≥70%（展示识别结果供用户手动修正）
- AC2.5: 用户点击"导出"时，可选择导出为 .docx 或 .pdf 格式，文件内容与当前简历一致
- AC2.6: 导出文件排版整洁，无乱码，符合正式投递标准

### AI简历智能分析
- AC3.1: 用户点击"AI智能分析"后，系统在5-15秒内返回分析结果（含加载中状态提示）
- AC3.2: 雷达图展示至少6个维度的评分：教育背景、工作经历、项目经验、技能匹配、成果量化、排版规范，每个维度0-100分
- AC3.3: 雷达图使用ECharts渲染，支持鼠标悬停查看具体分数
- AC3.4: 分析结果下方列出修改建议列表，每条建议包含：原文定位、问题描述、优化建议、修改示例
- AC3.5: 修改建议至少包含5条，覆盖至少4个维度
- AC3.6: 用户可点击"应用建议"一键将修改示例应用到简历中，或点击"忽略"跳过该条建议
- AC3.7: 分析记录存入数据库，用户可在历史记录中查看过往分析报告