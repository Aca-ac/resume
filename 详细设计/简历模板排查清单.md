# 简历模板排查清单（Sprint3 · AC3.1）

> 排查人：tian  
> 排查日期：2026-07-16  
> 自动化依据：`TemplateRenderAuditTest`、`JobIntentionRenderTest`、`DocxTemplateSanitizerTest`

## 1. 模板资产一览

| ID | 名称 | 分类 | classpath 路径 | 预览图 | 排查结果 |
|----|------|------|----------------|--------|----------|
| 1 | 简约风格 | 简约 | `/templates/resumes/simple/template.docx` | `/templates/resumes/simple/preview.png` | **正常** |
| 2 | 专业风格 | 专业 | `/templates/resumes/professional/template.docx` | `/templates/resumes/professional/preview.png` | **正常** |
| 3 | 创意风格 | 创意 | `/templates/resumes/creative/template.docx` | `/templates/resumes/creative/preview.png` | **正常** |
| 4 | 学术风格 | 学术 | `/templates/resumes/academic/template.docx` | `/templates/resumes/academic/preview.png` | **正常** |

## 2. 验收项

| 检查项 | 说明 | 结果 |
|--------|------|------|
| 模板文件存在 | classpath 下 docx + preview.png 成对存在 | 通过 |
| 占位符替换 | name/phone/email/jobIntention/各分段/summary 无残留 | 通过 |
| 浮动层清理 | 导出后无 wp:anchor 重叠（sanitizer） | 通过 |
| 求职意向 | `{{jobIntention}}` 四套均可渲染 | 通过 |
| PDF 导出 | 依赖 LibreOffice，与模板 docx 无关 | 环境依赖 |

## 3. 无法正常使用的模板

**当前无。** 四套模板均可完成 Word 渲染，未发现阻塞性缺陷。

## 4. 已知限制（非模板损坏）

1. **PDF 导出**：需本机安装 LibreOffice（`app.export.libreoffice-command`）。
2. **一寸照**：模板需含 photo 图片占位符；无照片时 sanitizer 清除残留。
3. **poi-tl 拆 run**：极端排版下 sanitizer 二次补全，若 Word 模板手写占位符名错误仍会原样保留。
4. **开发环境预览图**：前端需代理 `/templates/**` 到后端 8080。

## 5. 回归命令

```bash
cd server
mvn test -Dtest=TemplateRenderAuditTest,JobIntentionRenderTest,DocxTemplateSanitizerTest,TemplateExportServiceTest
```

## 6. 后续维护建议

- 新增模板时同步 Flyway `resume_templates` 记录与 `TemplateRenderAuditTest` 风格名。
- 占位符仅使用 camelCase：`jobIntention`、`workExperience` 等。
- 修改 docx 后在 Word 中目检导出效果，不单依赖单测。
