# Sprint 2 · 15 日上午验收速查

对照：`Sprint 2 阶段验收 要求.md`

## 交材料（带U盘/群文件）

| 材料 | 文件（Downloads） |
|------|-------------------|
| 质量门禁报告 | `Sprint2质量门禁完成报告_职通车.xlsx` |
| AI 抗性挑战报告 | `Sprint2AI抗性挑战完成报告_职通车.docx` |
| 证据 | 覆盖率/性能 Network 截图、MR 链接、导出 bug 对比图 |

### 单元测试 / 覆盖率（本地已跑通）

- 命令：`cd server && mvn test`
- 用例：**55** 跑通，**3** skip（集成测试无 LibreOffice/DB 时）
- **核心函数报告**（门禁用）：`server/target/site/jacoco-core/index.html` → **指令覆盖约 76%**（≥60%）
- 全量报告（含未测 Controller 等）：`server/target/site/jacoco/index.html`
- 边界场景示例：空岗位名 / fork 自己 / 路径穿越 `templatePath` / 空 SUMMARY / 错误导出格式 / 无主简历

分数逻辑：**功能演示 ≈ 60 分；门禁全过 → 冲 100；挑战 1–2 项额外分。**

## 讲师验收演示（约 8–10 分钟）

1. 登录 → 简历缩短保存 → 刷新仍在（SUMMARY）
2. 模板广场 → 预览 → **导出 Word/PDF**（确认不是 `api/api`）
3. 目标岗位 CRUD →（有 Key）AI 补全 → 社区 Fork + 评论（双号）
4. 简历匹配出分 + 历史；**勿夸「向量余弦 TOP2」**（未实现）
5. 翻开 Excel/Word 证据页

## 交叉 Review（结对同学组）

| 环节 | 时长 | 做什么 |
|------|------|--------|
| 准备问题 | 20 min | 可读对方代码（可用 AI），**自己写** 5–8 问，含「为什么 / 如果 / 没看懂」 |
| 质询会 | 15 min | 当面问／答，老师旁听 |
| 短报告 | 会后 | 给对方 3–5 条建议；你们改了可拿「改进分」 |

**本组备答（必会）：** `downloadUrl` 为何不能带 `/api`；SUMMARY 为何不取最长；模板静态资源为何只放行图片；Match 权重如何合成。

**可拿去问对方的举例（换成他们仓库路径）：**

- 为什么：鉴权为什么放在过滤器而不是方法注解？
- 如果：用户量×10，哪段同步 AI 调用会拖垮接口？
- 没看懂：请讲某段Prompt解析/错误码分支
