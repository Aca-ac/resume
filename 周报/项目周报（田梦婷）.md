#### **职通车周报**

**报告周期：** 2026-07-06 至 2026-07-12
**报告人：** 田梦婷
**报告日期：** 2026-07-12

---

**一、 基本信息**

*   **项目名称：** 职通车
*   **当前项目阶段：** Sprint1 开发（简历导入导出后端、AI 简历分析后端辅助功能已完成）
*   **本周总工时：** 50小时

---

**二、 本周完成工作**

*   **目标达成情况：**
    1. 简历导入导出模块（后端）：100%
    2. 简历导入导出模块数据库设计文档：100%
    3. AI 简历智能分析模块后端（历史记录、限流、结果解析）：100%

*   **具体完成任务列表：**

    **（一）简历导入导出模块（后端）**

    *   [任务1]：编写简历导入导出模块数据库设计文档 — 完成 `resumes` / `resume_details` / `resume_files` 三表结构设计，明确 V5 分层存储模型（元数据、分段正文、原文件/OCR 文本），输出文档 `详细设计/简历导入导出模块数据库设计.md`
    *   [任务2]：数据库表结构设计与迁移 — 设计简历主表（id、user_id、title、source_type、version、created_at、updated_at）与明细表（分段存储 SUMMARY/EDUCATION/WORK_EXPERIENCE/PROJECT/SKILL），配合 Flyway 迁移脚本落地
    *   [任务3]：文件导入接口开发 — 实现 Word/PDF 文本解析与 JPG/PNG 图片 OCR 识别（调用 DashScope `qwen-vl-plus`），支持 `/api/v1/resumes/import` 导入流程，OCR 结果写入 `resume_details` 供用户修正
    *   [任务4]：文件存储方案设计与实现 — 采用 UUID 文件命名 + 本地磁盘存储（`uploads/resumes/`），实现 MD5 秒传去重（`user_id + file_md5`），支持分片上传与合并（大文件场景）

    **（二）AI 简历智能分析模块后端 — 历史记录与限流**

    *   [任务5]：分析记录表结构设计 — 设计匹配分析记录表 `match_records`（含 user_id、resume_id、各维度评分、match_score、analysis、created_at）及索引，支持按用户/简历/时间查询
    *   [任务6]：分析历史记录查询接口 — 实现 `GET /api/v1/match/history` 分页查询用户匹配分析历史，匹配完成后写入数据库，前端可通过 `analysisId` 关联详情
    *   [任务7]：请求限流与结果解析 — 基于 Redis 实现 `@RateLimiter` 注解 + AOP 切面（匹配接口 20 次/分钟），封装大模型返回 JSON 解析（五维度评分、建议文本、异常兜底），统一错误提示

---

**三、 未完成工作及原因**

*   **未完成任务列表：**
    *   简历模板化导出（Word/PDF 按模板导出）
    *   **原因：** 按 Sprint1 计划延后至 Sprint2，本周优先完成导入与分析后端核心链路

---

**四、 遇到的问题与困难**

*   **技术问题：**
    *   DashScope API Key 配置来源不统一（`.env` 文件 vs 系统环境变量），本地启动时 OCR/分析接口偶发报「未配置 Key」
    *   **已尝试的解决方案：** 梳理 `application.yml` 中 `${DASHSCOPE_API_KEY}` 读取链路，确认系统环境变量可被 Spring Boot 正常加载；编写 `.env` 模板供本地参考
    *   **需要帮助或待解决：** 无
    *   合并 `main` 分支时出现 Java 文件冲突（如 `ResumeExportService.java`）
    *   **已尝试的解决方案：** 手动对比冲突内容，保留主分支功能后重新编译验证
    *   **需要帮助或待解决：** 无
*   **非技术问题：**
    *   部分接口缺少文档，联调时需要阅读源码才能理解入参/出参
    *   **已采取的措施：** 补充数据库设计文档，并在组内同步关键接口路径与字段说明

---

**五、 下周计划**

*   **主要目标：**
    *   完成 Sprint2 简历模板与模板化导出模块后端开发
*   **具体任务列表：**
    *   [任务1]：设计简历模板表结构，预计 0.5 天（5 小时）
    *   [任务2]：实现模板 CRUD 与模板预览接口，预计 1 天（10 小时）
    *   [任务3]：实现按模板导出 Word/PDF 接口，预计 1.5 天（15 小时）
    *   [任务4]：与前端联调模板选择与导出流程，预计 0.5 天（5 小时）

---

**六、 心得体会与学习收获**

*   **本周学习到的新知识/技能：**
    *   Flyway 数据库版本迁移 — 通过 V1~V9 迁移脚本管理表结构演进，避免手工改库
    *   DashScope 多模态 API — OCR 使用视觉模型 `qwen-vl-plus`，文本分析使用 `qwen-plus`，共用同一 API Key
    *   文件存储与秒传 — UUID 命名、MD5 去重、分片上传合并的完整实现思路
    *   Redis + AOP 接口限流 — 基于注解的通用限流方案，防止 AI 接口被滥用
*   **对项目或开发流程的思考：**
    *   数据库设计文档应尽早输出并与迁移脚本对齐，减少后期表结构返工；模块间接口文档同步也很重要，能显著降低联调成本
*   **自我改进方面：**
    *   合并主分支前先本地编译跑通，减少冲突处理时间
    *   敏感配置统一走环境变量，避免在代码或文档中硬编码密钥
