services:
  mysql:
    image: mysql:8.0
    container_name: resume-assistant-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-resume_assistant}
      MYSQL_USER: ${MYSQL_USER:-resume}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-resume123}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: resume-assistant-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    # Set REDIS_PASSWORD in .env and use: command: redis-server --requirepass ${REDIS_PASSWORD}

volumes:
  mysql_data:
  redis_data: